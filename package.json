{
  "name": "simple-react-native-game",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "clean": "rm -rf node_modules && rm -rf dist && rm -rf build && rm -rf .expo && rm -rf .expo-shared && rm -rf .expo-updates && rm -rf .expo-cache && rm -rf .expo-shared && rm -rf .expo-updates && rm -rf .expo-cache"
  },
  "dependencies": {
    "expo": "~54.0.24",
    "expo-app-loading": "^2.1.1",
    "expo-linear-gradient": "~15.0.7",
    "expo-splash-screen": "~31.0.11",
    "expo-status-bar": "~3.0.8",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-safe-area-context": "^5.6.2"
  },
  "private": true
}
